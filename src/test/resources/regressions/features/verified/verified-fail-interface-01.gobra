// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package pkg

// "verified" on an interface method signature is now accepted.
// The interface earns a domain axiom that callers can use in spec position.

type Getter interface {
	requires x > 0
	ensures  ret > 0
	decreases
	verified
	fetch(x int) (ret int)
}
